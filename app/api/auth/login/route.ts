import {NextResponse} from 'next/server';import {adminCookie,makeCookie,sameOrigin,verifyPassword} from '../../../../lib/server/auth';
export async function POST(request:Request){if(!sameOrigin(request))return NextResponse.json({error:'Origen inválido'},{status:403});const body=await request.json().catch(()=>({}));if(typeof body.password!=='string'||!verifyPassword(body.password))return NextResponse.json({error:'Contraseña incorrecta'},{status:401});const response=NextResponse.json({ok:true});response.cookies.set(adminCookie,makeCookie(),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'strict',path:'/',maxAge:604800});return response}
